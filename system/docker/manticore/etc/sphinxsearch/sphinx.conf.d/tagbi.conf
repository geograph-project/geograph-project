source srctagbi : db_slave
{
        sql_query               = \
                SELECT tag_id,tag,count as images \
                FROM tag_stat INNER JOIN tag USING (tag_id)
        sql_field_string        = tag
        sql_attr_uint        = images
}
index tagbi
{
        source                  = srctagbi
        path                    = /var/lib/manticore/data/tagbi
        dict = keywords
	min_infix_len = 2
	bigram_index = all
}
