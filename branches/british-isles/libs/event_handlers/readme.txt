Event Handlers
------------------------

This directory contains folders, each named after an event. When the application
fires such an event, the system scans the folder for handler classes for that
event - see the demo/ event folder for an example.